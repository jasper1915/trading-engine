spring.application.name=engine
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres?prepareThreshold=0
spring.datasource.username=postgres.pnjraiocynkhwshyroln
spring.datasource.password=${DB_PASSWORD:}

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

server.port=${PORT:8080}

server.error.include-message=always
server.error.include-binding-errors=always
spring.jpa.open-in-view=false

# ===============================
# 📧 AWS SES CONFIGURATION
# ===============================
# 📧 Brevo (Email)
brevo.api-key=${BREVO_API_KEY:your-brevo-api-key}
brevo.sender-name=Stockify
brevo.sender-email=noreply@stockify.best

# 📧 AWS SES (Email - Backup)
aws.access-key=${AWS_ACCESS_KEY:your-aws-access-key}
aws.secret-key=${AWS_SECRET_KEY:}
aws.region=ap-south-1
aws.sender-email=${AWS_SENDER_EMAIL:}

# ===============================
# 📱 MSG91 CONFIGURATION (SMS)
# ===============================
#msg91.auth-key=${MSG91_AUTH_KEY:}
#msg91.template-id=${MSG91_TEMPLATE_ID:}

# 📱 Twilio Settings (SMS Fallback)
twilio.account-sid=${TWILIO_SID:}
twilio.auth-token=${TWILIO_TOKEN:}
twilio.phone-number=${TWILIO_PHONE:}

# ===============================
# 🤖 GEMINI AI CONFIGURATION
# ===============================
# Get your free key at https://aistudio.google.com/
gemini.api-key=${GEMINI_API_KEY:}
gemini.model=gemini-flash-latest